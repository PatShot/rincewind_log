package command

// FUTUREWORK: Make this the Show Last Entries in DB Command.

// var initDBCmd = &cobra.Command{
// 	Use:   "initdb",
// 	Short: "Initializes the database",
// 	Long:  "Initializes the SQLite database with the required schema and indexes. This should be run before using any other commands that interact with the database.",
// 	Run:   runInitDB,
// }

// func runInitDB(cmd *cobra.Command, args []string) {
// 	err := database.InitDB("rincewind.db")
// 	if err != nil {
// 		fmt.Printf("Failed to initialize database: %v\n", err)
// 		return
// 	}
